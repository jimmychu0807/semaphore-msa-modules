import { Group, Identity, SemaphoreProof, generateProof } from "@semaphore-protocol/core"
import { loadFixture } from "@nomicfoundation/hardhat-toolbox/network-helpers"
import { expect } from "chai"
import { run } from "hardhat"

describe("SemaphoreValidator contract", function() {
  it("should call semaphoreValidator", async function() {
    console.log("Hardhat SemaphoreValidator test");
  });
});
